<Window x:Class="WpfTaskApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text Analyzer" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Введите текст:" Margin="5"/>
        <TextBox x:Name="InputTextBox" Grid.Row="1" Margin="5"/>

        <Button x:Name="AnalyzeButton" Grid.Row="2" Content="Анализировать" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" Click="AnalyzeButton_Click"/>

        <TextBlock x:Name="ReportTextBox" Grid.Row="3" TextWrapping="Wrap" Margin="5"/>
    </Grid>
</Window>
